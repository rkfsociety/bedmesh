# Анализ K3SysUi — 2026-08-27

- Экспериментальные копии `v6`–`v12` после полного перезапуска зависали на старте UI; восстановление выполнялось через штатный USB-SWU.
- В `v6`–`v10` внедрённый код находился в незамапленном промежутке ELF: первый исполняемый `PT_LOAD` заканчивался на виртуальном адресе `0x67c784`, с которого начинался патч.
- В `v10` были исправлены смещения вызовов `BL`, но этого было недостаточно из-за незамапленного сегмента.
- В `v11` первый `R-X` сегмент ELF был расширен, однако live-запуск всё равно завершался зависанием.
- В `v12` исправлена логика условного перехода в `AcSettingNozzleSelect`: для штатных индексов восстанавливался `mov r1,r2`, а строка `1.0` переносилась из зоны trampoline. Статическая проверка проходила, но live-запуск снова зависал.
- Последний отправленный файл: `K3SysUi.trial-add-1mm-both-v12-mapped`, MD5 `93a3a85ab6af19ef36f8be7675de85c0`. Перед заменой создана копия `/userdata/app/gk/K3SysUi.before-v12-20260827`.
- `start.sh` запускает `gklib`, `gkapi`, `K3SysUi` и `gkcam`; явной проверки MD5/подписи `K3SysUi` в нём не найдено.
- В `gklib` и `gkapi` присутствуют криптографические функции протокола, но это не подтверждает проверку файла UI.
- Официальный `ANYCUBIC-3D/Klipper-go` содержит backend-конфигурацию S1 (`nozzle_diameter`, PID и input-shaper), но исходников сенсорного UI `K3SysUi` не содержит.
- Следующий безопасный план: после восстановления снять свежие логи запуска и конфигурации, затем искать штатный API или ресурсный механизм расширения списка сопел. Прямое внедрение машинного кода остановлено до появления runtime-доказательств.

## Runtime-наблюдение штатной калибровки — 2026-08-27

- Во время штатной полной калибровки принтер не перезапускался; `gklib`, `gkapi`, `K3SysUi` и `gkcam` оставались запущены.
- `/tmp/gkui.log` зафиксировал последовательность: `Printer/ReportUIWorkStatus` с `busy:1`, затем `PidCalibrate/Extruder` с параметром `target:230`.
- `/tmp/gklib.log` подтвердил преобразование RPC в G-code: `PID_CALIBRATE HEATER=extruder TARGET=230 WRITE_FILE=0`; после 12 пиков калибровка завершилась, затем были записаны PID-параметры и вызван `SAVE_CONFIG`.
- Штатный UI получает фактические данные сопла через подписку `extruder`: `Nozzle_diameter: 0.4`, `Nozzle_material: hardened_steel`.
- На момент наблюдения `/userdata/app/gk/config/nozzle.cfg` содержал `material: "-"`, `diameter: "-"`, `modify: false`, тогда как `printer_mutable.cfg` содержал `nozzle_diameter: "0.40"` и `nozzle_material: "hardened_steel"`.
- Вывод: для будущего безопасного расширения нужно исследовать RPC/контроллер конфигурации и штатный ресурс UI; изменение только `nozzle.cfg` не является источником отображаемого состояния сопла.

## Runtime-срез полной калибровки после восстановления — 2026-08-27

- Снятие данных выполнялось только чтением по SSH, без перезапуска и без записи на принтер.
- После восстановления штатный `K3SysUi` имел MD5 `1bd84d3856b09a13a634143bb42378e5`; штатные процессы `gklib`, `gkapi`, `K3SysUi`, `gkcam` были запущены.
- Источники логов: `/tmp/gkui.log` и `/tmp/gklib.log`.
- При старте UI отправил `Query/Subscribe` для объекта `extruder`; RPC-статус вернул `Nozzle_diameter: 0.4` и `Nozzle_material: hardened_steel`.
- Полная цепочка, подтверждённая логами: `Printer/ReportUIWorkStatus busy=1` → `PidCalibrate/Extruder target=230` → G-code `PID_CALIBRATE HEATER=extruder TARGET=230 WRITE_FILE=0` → 12 пиков → `SAVE_CONFIG`.
- PID экструдера записан как `Kp=33.067`, `Ki=5.652`, `Kd=48.361`.
- Затем UI отправил `Resonance/SetShaperCalibrate` со скриптом `G28 W` и `SHAPER_CALIBRATE AXIS=x`; результат X: `3hump_ei`, `66.4`.
- Вторым запросом UI отправил `SHAPER_CALIBRATE AXIS=y`; результат Y: `2hump_ei`, `59.0`.
- После Y UI снова вызвал `Config/PrinterConfSave` с `SAVE_CONFIG`.
- На момент среза `nozzle.cfg` оставался `{material: "-", diameter: "-", modify: false}`, `printer.cfg` содержал `nozzle_diameter : 0.400`, а `printer_mutable.cfg` содержал `nozzle_diameter: "0.40"` и `nozzle_material: "hardened_steel"`.
- Ключевой вывод: отображение сопла и запуск калибровки связаны с RPC-статусом `extruder` и контроллером конфигурации; `nozzle.cfg` является отдельным флагом состояния и не единственным источником данных.

## Посткалибровочный итог — 2026-08-27

- Срез снят только чтением после полного завершения; перезапусков и записей на принтер не выполнялось.
- Штатные процессы после завершения живы: `gklib -a /tmp/unix_uds1 printer.cfg`, `gkapi`, `K3SysUi`, `gkcam`.
- Штатный UI не изменён: `/userdata/app/gk/K3SysUi` MD5 `1bd84d3856b09a13a634143bb42378e5`.
- UI завершил цепочку с `Printer/ReportUIWorkStatus busy=0`.
- Полная последовательность подтверждена: `PID_CALIBRATE` экструдера → `SAVE_CONFIG` → `G28 W` + `SHAPER_CALIBRATE AXIS=x` → `SHAPER_CALIBRATE AXIS=y` → `SAVE_CONFIG` → `LEVIQ2_PREHEATING` → `LEVIQ2_WIPING` → `LEVIQ2_PROBE` → `SAVE_CONFIG` → отключение нагрева стола/сопла → `busy=0`.
- Итог шейперов: X `3hump_ei`, `66.4`; Y `2hump_ei`, `59.0`.
- Итог bed mesh: сетка `5×5`, `min_x=5`, `max_x=245`, `min_y=5`, `max_y=245`, `mesh_x_pps=2`, `mesh_y_pps=2`, алгоритм `lagrange`, `tension=0.2`.
- Итоговые точки bed mesh сохранены в `printer_mutable.cfg`: `0.565500, 0.232500, 0.276667, 0.247000, 0.489500 / 0.332667, 0.042667, 0.042000, 0.107000, 0.484667 / 0.149500, 0.013167, -0.007000, 0.127333, 0.444333 / -0.081667, -0.014167, 0.179167, 0.214000, 0.362000 / -0.191500, 0.211333, 0.523833, 0.513667, 0.587000`.
- Итог сопла не изменился: `nozzle_diameter=0.40`, `nozzle_material=hardened_steel`; `nozzle.cfg` по-прежнему содержит `material: "-"`, `diameter: "-"`, `modify:false`.
- Итог PID экструдера: `Kp=33.067`, `Ki=5.652`, `Kd=48.361`.
- В логах есть предупреждение `Can't read autosave from config file - modifications after header`; при этом новые значения фактически присутствуют в `printer_mutable.cfg`, а процессы и RPC работают штатно. Это нужно учитывать при дальнейшем анализе сохранения конфигурации.
- Ключи поиска в логах: `/tmp/gkui.log`, `/tmp/gklib.log`, `PidCalibrate/Extruder`, `Resonance/SetShaperCalibrate`, `Leviq2/Preheating`, `Leviq2/Wiping`, `Leviq2/Probe`, `Config/PrinterConfSave`, `Printer/ReportUIWorkStatus`.

## Безопасный runtime-срез после восстановления — 2026-08-27, 10:19 UTC

- Проверка выполнялась только чтением по SSH на `192.168.63.179:2222`; перезапусков, остановки процессов и записи на принтер не выполнялось.
- Система отвечает: Linux Rockchip `5.10.160`, `armv7l`; принтер работает около 17 минут на момент проверки.
- Все штатные процессы живы: `gklib -a /tmp/unix_uds1 printer.cfg` (PID 249), `gkapi` (PID 356), `K3SysUi` (PID 357), `gkcam` (PID 382); также работают `gkbridge` и SSH dropbear на 2222.
- Контрольные суммы штатных бинарников совпали с восстановленной копией: `K3SysUi` `1bd84d3856b09a13a634143bb42378e5`, `gklib` `0d769218b5dcd2cded34372ec14fd820`, `gkapi` `efa64b0be1a947e34127379986fc7257`.
- Текущий RPC-статус `extruder` по-прежнему сообщает `Nozzle_diameter: 0.4` и `Nozzle_material: hardened_steel`; состояние Klipper — `ready`.
- Текущий `/userdata/app/gk/config/nozzle.cfg` содержит `material:"-"`, `diameter:"-"`, `modify:false`; это согласуется с отсутствием автоматической полной калибровки после восстановления.
- В `printer.cfg` активен `nozzle_diameter : 0.400`; в `printer_mutable.cfg` сохранены `nozzle_diameter: "0.40"` и `nozzle_material: "hardened_steel"`, а также результаты последней калибровки.
- `start.sh` и `appCheck.sh` запускают и контролируют процессы, но в просмотренных скриптах нет проверки MD5 или подписи `K3SysUi`.
- Локальный файл `config/nozzle.cfg` в старом каталоге эксперимента содержит тестовые значения `hardened_steel/1.00`; он не является свежей копией текущего принтера и не должен использоваться как источник состояния.

## Статический разбор штатной логики выбора сопла — 2026-08-27

- В таблице символов штатного `K3SysUi` найдены: `MainWindow::AcSettingNozzelReflash` (формирует 8 записей), `MainWindow::AcSettingNozzleSelect`, `MainWindow::ExtruderDiameter`, `ParaConfig::ConfigNozzleSize`, `ParaConfig::IsNozzleChanged`, `RpcApi::CmdSetPrintExtruDiameter` и `RpcParser::ParserResponeSetNozzleDiameter`.
- `AcSettingNozzelReflash` явно задаёт размер списка `8`; обработчик выбора принимает индексы `0..7` и сопоставляет их с таблицей значений диаметра.
- `AcSettingNozzleSelect` после выбора вызывает `RpcApi::CmdSetPrintExtruDiameter` с выбранным `double` и текстовым `QString`; это штатный путь изменения диаметра, а не простая перерисовка подписи.
- `ParserResponeSetNozzleDiameter` разбирает ответ RPC, обновляет глобальный `NozzleDiameter` и передаёт значение в `MainWindow::ExtruderDiameter`; поэтому корректное отображение требует согласованного изменения RPC/состояния, а не только добавления строки.
- В извлечённых символах нет отдельного штатного метода изменения `Nozzle_material`; материал, вероятно, выбирается парой в UI и/или сохраняется через конфигурационный контроллер. Это нужно подтвердить чтением RPC-обмена, не вызывая изменяющие методы.
- Вывод для следующего этапа: исследовать формат параметров `Printer/SetExtruDiameter`, вызовы конфигурационного контроллера и встроенные Qt-ресурсы; прямое внедрение машинного кода в `K3SysUi` остаётся остановленным из-за повторяющихся зависаний при запуске.

## Уточнение RPC выбора диаметра — 2026-08-27

- Статическим разрешением относительных адресов подтверждено наличие строк `method`, `params` и `Printer/SetExtruDiameter` в коде `RpcApi::CmdSetPrintExtruDiameter`.
- Штатный обработчик ответа `RpcParser::ParserResponeSetNozzleDiameter` ищет ключи `Nozzle_diameter` и `Nozzle_material`; после этого обновляет глобальное состояние диаметра и вызывает `MainWindow::ExtruderDiameter`.
- Вызов из `MainWindow::AcSettingNozzleSelect` передаёт в `CmdSetPrintExtruDiameter` выбранный `double` и `QString`; отдельного штатного RPC-setter для `Nozzle_material` среди найденных символов нет.
- Следовательно, расширение списка до новых комбинаций должно учитывать не только UI-строки и размер контейнера `8`, но и backend-схему ответа/сохранения материала. Одной подменой `8` или одной новой строкой корректное отображение и выбор не обеспечиваются.
- Безопасные команды для дальнейшего исследования: только запросы состояния/чтения RPC и чтение файлов. Нельзя вызывать `Printer/SetExtruDiameter`, `Config/PrinterConfSave`, `SAVE_CONFIG`, калибровки, движение, нагрев или перезапуск без отдельного подтверждения.

## Таблица сопел и Qt-ресурсы — 2026-08-27

- В штатном ELF таблица `double` для `AcSettingNozzleSelect` найдена по виртуальному адресу `0x3c0928`; восемь значений: `0.2, 0.25, 0.4, 0.6, 0.8, 0.4, 0.6, 0.8`.
- Порядок штатных пар: `Brass-0.2`, `Brass-0.25`, `Brass-0.4`, `Brass-0.6`, `Brass-0.8`, затем `Hardened Steel-0.4`, `Hardened Steel-0.6`, `Hardened Steel-0.8`.
- Строки подписей находятся рядом в `.rodata`: `Brass`, `Hardened Steel`, `0.2`, `0.25`, `0.4`, `0.6`, `0.8`; повторения последних диаметров используются для второй группы материала.
- Для добавления `Brass-1.0` и `Hardened Steel-1.0` потребуется расширить минимум размер контейнера `8` до `10`, таблицу диаметров до 10 элементов, генерацию двух новых строк и обработку индексов. Нужно также проверить, как выбранный материал передаётся в backend.
- В бинарнике присутствуют встроенные Qt-ресурсы `qrc_res.cpp` и `qrc_qss.cpp` (`qt_resource_data`, `qt_resource_name`, `qt_resource_struct`), но в таблице имён нет отдельного ресурса списка сопел или изображения `nozzle-1.0`; внешний файл для простой подмены списка не найден.
- Из-за этого прежние live-попытки с машинным кодом нельзя считать безопасным методом: статическая корректность не гарантирует совместимость Qt/C++ ABI и уже приводила к зависанию UI. Следующий эксперимент должен быть только на локальной копии или через штатный расширяемый API, если такой будет найден.

## Найден путь сохранения материала и флага калибровки — 2026-08-27

- `ParaConfig::ConfigNozzleSize` статически разрешает строки `material`, `diameter`, `modify`, путь `/config/nozzle.cfg` и команду `sync`.
- Метод принимает два текстовых значения сопла и записывает JSON-метаданные в `/userdata/app/gk/config/nozzle.cfg`; это не внешний список, а штатный маркер состояния перед загрузкой.
- В `AcSettingNozzleSelect` найдены штатные строки материалов `brass` и `hardened_steel`, а вызов `ConfigNozzleSize` передаёт флаг `1` (`modify=true`) при выборе нового пункта. Это и есть подтверждённый триггер полной калибровки после следующего полного запуска.
- После успешного выбора штатный UI отдельно вызывает `Printer/SetExtruDiameter`, чтобы обновить диаметр через RPC; обработчик ответа обновляет `Nozzle_diameter`/`Nozzle_material`.
- Безопасный режим приложения уже найден ранее: записать новые `printer.cfg` и `nozzle.cfg` с `modify:false`, затем выполнить полный перезапуск без калибровки. Для имитации штатного экрана с новыми пунктами нужно будет расширить список и передавать материал/диаметр в эти два штатных пути согласованно.

## Жизненный цикл списка в исходном модуле — 2026-08-27

- В таблице символов присутствует имя исходного модуля `AcNozzelSize.cpp`; функции `AcSettingNozzelUiInit`, `AcSettingNozzelReflash` и `AcSettingNozzleSelect` относятся к одному компоненту.
- `AcSettingNozzelReflash` последовательно создаёт восемь пар текстовых значений, передаёт их Qt-контейнеру и затем освобождает временные строки. Это подтверждает, что список строится программно при обновлении экрана.
- `AcSettingNozzelUiInit` подключает сигнал выбора `QAbstractItemView::clicked(QModelIndex)` к лямбда-обработчику; выбор строки напрямую приводит к `AcSettingNozzleSelect`.
- Встроенный ресурсный модуль содержит изображения и QSS, но не отдельный конфигурационный файл списка сопел. Для расширения до 10 пунктов понадобится менять машинный код/сборку компонента, а не только ресурс.

## Карта активного и резервных UI-файлов — 2026-08-27

- Активный `/userdata/app/gk/K3SysUi` имеет штатный MD5 `1bd84d3856b09a13a634143bb42378e5`.
- `/userdata/app/gk/K3SysUi.1` имеет MD5 `93a3a85ab6af19ef36f8be7675de85c0` и соответствует ранее отправленной аварийной версии v12; файл сейчас не запущен.
- `/userdata/app/gk/K3SysUi.stock-backup-20260827`, `K3SysUi.original.bedmesh_20260827_090503` и `before-v10/v11/v12` совпадают со штатным MD5 `1bd84d...`.
- Остаточные файлы `before_gdb_*` и `trial_before_add_*` имеют другой старый MD5; их нельзя считать штатными без отдельной проверки.
- Файл `K3SysUi.1` скачан в локальный снимок только для анализа; на принтере ничего не заменялось и не удалялось.

## Проверка резервных сборок и внешнего поиска — 2026-08-27

- Все локальные копии `K3SysUi`, включая штатную, `K3SysUi.1` и экспериментальные v6-v12, содержат одну и ту же последовательность пяти первых диаметров `0.2, 0.25, 0.4, 0.6, 0.8` в таблице выбора; готовой сборки с десятью пунктами среди них нет.
- Поиск по уникальным идентификаторам `AcNozzelSize.cpp`, `AcSettingNozzelReflash` и `Printer/SetExtruDiameter` во внешнем доступном поиске исходников результатов не дал.
- Внешних исходников сенсорного UI или официальной расширенной сборки не обнаружено; текущая доказательная база указывает на необходимость локального тестового стенда/пересборки либо очень осторожной модификации закрытого ELF.
- Эксперименты с заменой активного бинарника на принтере до появления локальной runtime-проверки не продолжать.

## Исследование Rinkhals — 2026-08-27

- Проверен предоставленный клон `C:\github\Rinkhals`, ревизия `78c458e2` (`Merge develop into master (release 20260716_02)`). В репозитории есть исходный механизм анализа и патчинга закрытых Anycubic-бинарников, но нет исходников `K3SysUi` или `AcNozzelSize.cpp`.
- Для Kobra S1 в репозитории присутствуют скрипты патчей `K3SysUi.KS1_<firmware>.sh` и `gkapi.KS1_<firmware>.sh`, включая `K3SysUi.KS1_2.7.2.7.sh`. Патчи создаются из оригинального бинарника через `create-patch.py`, сравниваются с оригиналом и превращаются в скрипт с проверкой MD5 и точечными `dd`-записями.
- Патч Rinkhals для `KS1 2.7.2.7` ожидает исходный MD5 `1bd84d3856b09a13a634143bb42378e5`, то есть совпадает с подтверждённым штатным `K3SysUi` принтера. После применения ожидается MD5 `e4f32d218e073b43eafc7d5e98ee5293`.
- По коду `create-patch.py` видно, что Rinkhals патчит `AcSupportRefresh`, перехватывает обработчик пункта `Service Support`, добавляет машинный код и строки запуска внешнего `rinkhals-ui.sh`, затем генерирует бинарный diff. Это готовый пример patch-at-known-offsets с защитой от неправильной версии.
- Конкретный патч Rinkhals не расширяет список сопел, не меняет `AcSettingNozzelReflash`, `AcSettingNozzleSelect`, таблицу диаметров или RPC `Printer/SetExtruDiameter`; он только переиспользует пункт Service Support/Support для запуска меню Rinkhals. Применять его к принтеру без отдельного решения не следует: он изменит пользовательский экран и займёт собственные участки кода.
- В документации Rinkhals подтверждено: `K3SysUi` — закрытый C/C++ UI, `gklib` — GoKlipper, `gkapi` — API/оркестратор; проект предоставляет инструменты Ghidra/Binary Ninja и готовые модельно-версионные патчи, но не исходный UI.
- Практический вывод: Rinkhals даёт нам проверенный формат доставки/отката и пример генерации патча, а не готовое решение сопел. Для `Brass/Hardened Steel-1.0` всё равно нужно локально исследовать и патчить собственные функции списка, причём не смешивать этот патч с Rinkhals-перехватом без карты свободного места и проверки всех перекрытий.
- Во время этого исследования принтер не изменялся, RPC записи, перезапуск и калибровки не выполнялись. Секреты и пароли из внешних материалов в проектную память не записывались.
- В Rinkhals есть отдельный экран `rinkhals-ui.py` на LVGL/Python: он умеет создавать собственные экраны, кнопки, списки и диалоги выбора/редактирования; штатный `K3SysUi` запускает его через перехваченный пункт `Service Support`. Это делает отдельную вкладку сопла технически возможной при установленном Rinkhals.
- Документированный IPC Rinkhals описывает UNIX-сокет `/tmp/unix_uds1`, TCP-прокси `gkapi` на порту `18086`, запросы состояния filament hub и LED, но не содержит готовой команды для `Printer/SetExtruDiameter` или для сохранения `nozzle_material`. Поэтому канал для будущей вкладки ещё нужно подтвердить на нашем бинарнике read-only трассировкой/списком endpoint'ов.
- В истории Rinkhals нет отдельного nozzle/diameter-модуля или коммита, который расширяет штатную таблицу сопел. Полезная часть проекта для нас: LVGL UI-слой, точечный ARM-патч с проверкой исходного MD5 и методика анализа IPC; готовые данные сопел отсутствуют.
- В удалённых ветках Rinkhals найден ещё более подходящий прототип `feature/K3SysUi_z_offset_UI`: `libzoffset.so` подключается к штатному `K3SysUi` через `LD_PRELOAD`, перехватывает `QButtonGroup::addButton`, создаёт новую Qt-кнопку и не заменяет ELF. В README описаны runtime-защита SIGSEGV/sigsetjmp, opt-in переменные окружения и динамическое обнаружение смещения объекта.
- В удалённой ветке `feature/dynamic-patching` найден Go-патчер: он проверяет ELF, динамически ищет символы/dispatcher/jump-table и при ошибке отказывается патчить. Это лучше старых фиксированных адресов, но он всё ещё предназначен для меню Rinkhals, а не для списка сопел.
- Эти ветки не являются готовым решением для нашего принтера и не должны автоматически сливаться в BedMesh. Они дают архитектурный ориентир: сначала отдельный внешний UI/shim с безопасным read-only режимом, затем подключение записи конфигурации только после подтверждения IPC и резервного восстановления.
- Поиск по всем локально доступным веткам/тегам Rinkhals и внешним GitHub-обсуждениям не нашёл реализации выбора диаметра сопла. Единственное близкое обсуждение — пользовательский профиль Orca для 0.8 мм на KS1 2.7.2.7; обсуждение осталось без ответа и не содержит экранного/API-решения.
- Последняя проверенная стабильная ревизия клона — `78c458e2`/релиз `20260716_02`; доступный test-релиз `20260726_01_test` посвящён другим исправлениям и не добавляет nozzle UI. Внешняя документация Rinkhals продолжает описывать только общий выбор пресета сопла в Orca, а не управление соплом на экране принтера.

## Исследование официального Klipper-go — 2026-08-27

- Скачан полный архив публичного репозитория `ANYCUBIC-3D/Klipper-go` для локального read-only анализа; распакованный снимок хранится вне репозитория проекта в `C:\Users\USSR\Downloads\Klipper-go-unpacked-20260827`.
- В открытом коде `project/kinematics_extruder.go` поле `Nozzle_diameter` читается из конфигурации `nozzle_diameter` и публикуется в статусе. Диаметр используется для ограничений экструзии, но отдельного `nozzle_material` и команды динамической смены диаметра в найденном исходном коде нет.
- В конфигурациях Anycubic найдена статическая строка `nozzle_diameter : 0.400`; готового списка экранных пресетов, `Printer/SetExtruDiameter` или обработчика выбора материала в открытой части репозитория нет.
- Открытый Klipper-go содержит общие `SAVE_CONFIG`, `RESTART` и `FIRMWARE_RESTART`; значит, базовый уровень Klipper предполагает сохранение конфигурации и перезапуск, но логика штатного экрана и флага `modify` остаётся в закрытых `gkapi`/`K3SysUi`.
- Итог поиска: официальный репозиторий полезен для понимания формата и последствий `nozzle_diameter`, но не даёт готовой реализации новых пунктов UI. Наиболее безопасное направление остаётся прежним: локально прототипировать отдельный UI/bridge в read-only режиме, затем отдельно подтвердить запись диаметра и материала через штатный API, не отправляя бинарные эксперименты на принтер.
- В этом цикле принтер не изменялся, перезапусков и калибровок не выполнялось. Пароли, ключи и другие секреты в память проекта не записывались.

## Read-only проверка endpoint'ов Kobra S1 — 2026-08-27

- Через SSH-порт `2222` выполнен только локальный read-only запрос `list_endpoints` к gkapi на `127.0.0.1:18086`; прямое подключение к `192.168.63.179:18086` с ПК отклоняется, потому что этот TCP endpoint слушает loopback на принтере.
- Фактический список endpoint'ов содержит `print/set_extru_diameter`, а также `objects/query`, `objects/subscribe`, `config/save_config`, `gcode/restart` и `gcode/firmware_restart`. Наличие изменяющих endpoint'ов зафиксировано как справочная информация; в этом исследовании они не вызывались.
- В опубликованном списке нет отдельного endpoint'а для `nozzle_material`; это согласуется с ELF-анализом: диаметр проходит через штатный RPC, а материал сохраняется отдельной конфигурационной логикой `K3SysUi`/gkapi.
- `objects/query` и `objects/list` через этот proxy в текущем формате не вернули полезный объект сопла (`objects:null`/пустой ответ), поэтому результат не считать доказательством отсутствия состояния; для следующего этапа нужен точный формат запроса, снятый штатным UI или из IPC-трассы.
- Документация Rinkhals отдельно предупреждает, что прямое изменение штатного `printer.cfg` может привести к ошибке запуска GoKlipper; при установленном Rinkhals используется собственная цепочка конфигурации. Это исключает безопасное предположение, что Rinkhals автоматически решит запись новых сопел в штатную конфигурацию.
- Практический следующий шаг для локального прототипа: не менять принтер, а реализовать mock/protocol harness для чтения статуса, проверки формата `print/set_extru_diameter` на тестовом сокете и отображения пар `материал + диаметр`; запись на реальный принтер оставить отдельным подтверждаемым этапом.
- В этом цикле выполнены только подключения и чтение endpoint'ов; записи, нагрев, движение, калибровки и перезапуски не выполнялись.

## Статическая карта цепочки set nozzle — 2026-08-27

- В сохранённом Go ELF `gkapi` через `.gopclntab` подтверждены внутренние функции `printerApi/controller/printer.PrinterController.SetExtruDiameter` (value-wrapper, VA `0x363650`) и `printerApi/controller/printer.(*PrinterController).SetExtruDiameter` (основной метод, VA `0x367208`). Это позволяет анализировать обработчик локально без вызова изменяющего endpoint'а.
- В сохранённом `gklib` через `.gopclntab` подтверждён backend-метод `k3c/internal/pkg/toolhead.(*PrinterExtruder).setNozzleDiameter` (VA `0x1ca864`). Таким образом, найденная цепочка имеет вид `K3SysUi -> gkapi print/set_extru_diameter -> gklib PrinterExtruder.setNozzleDiameter`.
- В `K3SysUi::CmdSetPrintExtruDiameter` подтверждены два входа: `double` диаметра и `QString` материала/идентификатора. Метод формирует RPC-пакет `Printer/SetExtruDiameter`; точные имена полей запроса ещё требуют подтверждения по сериализации или штатной IPC-трассировке.
- Read-only `gcode/help` не показывает отдельной G-code-команды для смены диаметра; доступен именно специализированный RPC endpoint. Это дополнительно подтверждает, что будущая вкладка должна работать через RPC/конфигурационный bridge, а не через обычный G-code.
- Поиск в официальном открытом репозитории `ANYCUBIC-3D/Klipper-go` и всех refs предоставленного Rinkhals не обнаружил реализации выбора материала/расширения экранного списка. Найденная карта закрытой цепочки — новый ориентир для локального harness и будущего UI.
- В этом цикле выполнены только статический анализ и read-only запросы `list_endpoints`/`gcode/help`; изменяющие RPC и команды не вызывались.

## Проверка официальных ограничений диаметра — 2026-08-27

- На актуальных официальных страницах Anycubic для Kobra S1 указано: штатный диаметр `0.4 mm`, дополнительные поддерживаемые диаметры `0.25 / 0.6 / 0.8 mm`; диаметр `1.0 mm` в спецификации не заявлен.
- Это не запрещает исследовать отображение `1.0` в UI или локальный профиль слайсера, но такой пункт нельзя выдавать за официально поддерживаемый режим. Перед реальной печатью потребуется отдельная проверка пропускной способности hotend, температурного профиля, максимальной ширины линии и механической совместимости.
- Для проекта разделить два понятия: `supported_stock` (0.25/0.4/0.6/0.8) и `experimental_custom` (например, 1.0). Экспериментальные значения не должны автоматически попадать в штатную прошивку или запускать live-тест.
- В этом цикле принтер не изменялся; выполнены только локальный поиск и чтение публичных спецификаций.
